import React from 'react';

const HomeFooter = () => {
  return (
    <footer style={footerStyle}>
      
    </footer>
  );
};

// Estilos en línea (opcional)
const footerStyle = {
  background: '#282c34',
  color: 'white',
  padding: '20px',
  textAlign: 'center',
  position: 'fixed',
  bottom: 0,
  width: '100%',
};

export default HomeFooter;
